<%= form_with model: current_user.tweets.build, local: :true do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field p-2">
    <%= f.text_area :content, placeholder: "25words change your world", maxlength: 25, class:"form-control", rows:"1"%>
  </div>
  <div class="row">
    <div class="col-md-6 col-lg-6">
      <div class="file p-1">
        <%= f.file_field :image, class: "folat-left" %>
      </div>
    </div>
    <div class="col-md-6 col-lg-6">
      <div class="col-md-4 offset-md-5 col-lg-4 offset-lg-6 pb-1">
        <%= f.submit "Post", class: "btn btn-success btn-lg" %>
      </div>
    </div>
  </div>
<% end %>
