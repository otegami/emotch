<%= form_with(url: emotions_path, remote: true) do |f| %>
  <div><%= f.hidden_field :tweet_id, :value => tweet.id %></div>
  <%= button_tag '<i class="fas fa-quote-right fa-2x", style="color: white"></i>&emsp;エモい'.html_safe, :type => "submit",
    :class =>"btn btn-default btn-s-md text-white" %>
<% end %>
